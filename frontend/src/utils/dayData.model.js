export default class DayDataModel {
    constructor(day, weatherDesc, temp, icon) {
      this.day = day;
      this.weatherDesc = weatherDesc;
      this.temp = temp;
      this.icon = icon;
    }
  }